% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_data_function.R
\name{generate_data_function}
\alias{generate_data_function}
\title{Generate a labeled two-sensor synthetic dataset}
\usage{
generate_data_function(
  n,
  add_background = TRUE,
  background_type = c("Poisson Moving Average", "Random Walk", "Sine Wave",
    "AR(1) Process"),
  poisson_k = 5L,
  poisson_lambda = 3,
  background_rho_rw = 0,
  randomwalk_sd = 0.1,
  sine_amplitude = 1,
  sine_period = 20L,
  background_phi = 0.8,
  background_rho = 0,
  delayed_sensor = c("None", "Sensor1", "Sensor2"),
  alpha_ema = 0.3,
  sd1 = 0.1,
  sd2 = 0.1,
  crosscor_noise = 0,
  mean1 = 0,
  mean2 = 0,
  n_spikes_corr = 0L,
  n_spikes_s1 = 0L,
  n_spikes_s2 = 0L,
  max_spike_length = 5L,
  n_drifts_s1 = 0L,
  n_drifts_s2 = 0L,
  drift_duration = c(20L, 40L),
  drift_slope = c(-0.05, 0.05)
)
}
\arguments{
\item{n}{Integer length (min 100; clamped).}

\item{add_background}{Logical; include background signal.}

\item{background_type}{Character. One of the supported background models.}

\item{poisson_k, poisson_lambda}{See \code{\link[=generate_background_function]{generate_background_function()}}.}

\item{background_rho_rw, randomwalk_sd}{See \code{\link[=generate_background_function]{generate_background_function()}}.}

\item{sine_amplitude, sine_period}{See \code{\link[=generate_background_function]{generate_background_function()}}.}

\item{background_phi, background_rho}{See \code{\link[=generate_background_function]{generate_background_function()}}.}

\item{delayed_sensor}{Character. One of "None","Sensor1","Sensor2".}

\item{alpha_ema}{Numeric in (0,1]; EMA coefficient when a sensor is delayed.}

\item{sd1, sd2}{Numeric > 0. Noise standard deviations.}

\item{crosscor_noise}{Numeric in \link{-1, 1}. Correlation between sensor noise terms.}

\item{mean1, mean2}{Numeric. Target means added via noise shift.}

\item{n_spikes_corr}{Integer >= 0. Number of correlated spike windows.}

\item{n_spikes_s1, n_spikes_s2}{Integers >= 0. Uncorrelated spikes per sensor.}

\item{max_spike_length}{Integer >= 1. Max spike window length.}

\item{n_drifts_s1, n_drifts_s2}{Integers >= 0. Uncorrelated drift windows per sensor.}

\item{drift_duration}{Integer vector length 2, c(min, max), min >= 2.}

\item{drift_slope}{Numeric vector length 2, c(min, max). Linear drift slope range.}
}
\value{
A data frame with columns: \code{TimeSinceClean}, \code{Date}, \code{Sensor1}, \code{Sensor2},
\code{Measurand1}, \code{Measurand2}, \code{AnomalyFlag1}, \code{AnomalyFlag2}, and \code{Diff}.
}
\description{
Generate a labeled two-sensor synthetic dataset
}
